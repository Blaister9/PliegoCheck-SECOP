# Report templates v1

Templates deterministas para Microfase 9. No ejecutan codigo, no usan IA y solo admiten reemplazos
controlados `{{campo}}` desde snapshots persistidos. Todo valor dinamico del HTML se escapa antes de
renderizarse.
