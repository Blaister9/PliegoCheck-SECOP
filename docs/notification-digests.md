# Digests

`DAILY_DIGEST` y `WEEKLY_DIGEST` agrupan alertas no archivadas por destino y periodo, respetan monitor, zona horaria configurada y límite de elementos. La unicidad destino-periodo evita duplicados. Sin alertas no se crea mensaje. El digest no sustituye el historial interno.
